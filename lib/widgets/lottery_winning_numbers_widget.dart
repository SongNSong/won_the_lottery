import 'package:flutter/material.dart';

class LotteryWinningNumbersWidget extends StatelessWidget {
  const LotteryWinningNumbersWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
